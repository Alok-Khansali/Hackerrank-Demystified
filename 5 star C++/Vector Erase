#include <bits/stdc++.h>
using namespace std;
int main()
{
     int n,m;
     cin>>n;
     vector<int> v(n);
     for(int i=0;i<n;i++)
       cin>>v[i];
     cin>>m;
      v.erase(v.begin()+(m-1));
      cin>>n>>m;
      v.erase(v.begin()+n-1,v.begin()+(m-1));
     cout<<v.size()<<'\n';
     for (int i=0;i<v.size();i++) 
        cout<<v[i] << " "; 
     return 0;
}
