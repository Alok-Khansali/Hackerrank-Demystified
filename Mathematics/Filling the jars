#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    long long n,m,s=0;
    cin>>n>>m;
    for(int i=0;i<m;i++)
    {
        long long a,b,k;
        cin>>a>>b>>k;
          s+=((b-a+1)*k);
    }
    cout<<s/n;
    return 0;
}

